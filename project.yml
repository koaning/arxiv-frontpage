title: "FrontPage: An Prodigy project to make a personal front-page."
description: "Using Prodigy, spaCy, and friends ... this project allows you to make your own frontpage of the internet."

# Variables can be referenced across the project.yml using ${vars.var_name}
vars:
  annot_stream: "raw/content.jsonl"
  site_folder: "./docs"
  model_folder: "./training"

# These are the directories that the project needs. The project CLI will make
# sure that they always exist.
directories: ["scripts", "assets", "data", "training", "raw"]

# Assets that should be downloaded or available in the directory. You can replace
# this with your own input data.
assets:
  - dest: ""
    description: ""

# Project commands, specified in a style similar to CI config files (e.g. Azure
# pipelines). The name is the command name that lets you trigger the command
# via "spacy project run [command] [path]". The help message is optional and
# shown when executing "spacy project run [optional command] [path] --help".
commands:
  - name: "download"
    help: "Download data from sources."
    script:
      - python -m frontpage download
    deps:
      - frontpage/_download.py
    outputs:
      - downloads
  - name: "preprocess"
    help: "Process data into Prodigy format."
    script:
      - python -m frontpage preprocess
    deps:
      - downloads
      - scripts/preprocess.py
    outputs:
      - ${vars.annot_stream}
      - indices
  - name: fetch-annot
    help: "Fetch annotations from ptc"
    script:
      - ptc datasets export 74e149a1-6fad-4a03-b4e0-f056cfe16ce1 --output annot/prompt-eng.jsonl
      - ptc datasets export 19293796-5755-4ad5-a728-23dec2f95a1c --output annot/new-dataset.jsonl
      - ptc datasets export 8aa23d32-35fd-40d6-83b6-d43a6518484f --output annot/data-quality.jsonl

workflows:
  fetch:
    - download
    - preprocess